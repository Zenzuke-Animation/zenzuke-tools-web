/**
 * Ajustes del sitio. Todo lo que se puede cambiar sin tocar la lógica.
 */
export const CONFIG = {
  catalogUrl: "scripts.json",
  langStorageKey: "zen_scripts_lang",
  fallbackLang: "en",
  scrollDelayMs: 200,
};
